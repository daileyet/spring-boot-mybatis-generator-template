# sprj-web

## run as default

```shell
mvn spring-boot:run
```

## package as Jar

```shell
mvn clean install
``` 

## package as War

```shell
mvn clean install -Dpackaging.style=war
``` 